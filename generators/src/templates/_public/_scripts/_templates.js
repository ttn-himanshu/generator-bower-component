angular.module('myapp').run(['$templateCache', function($templateCache) {
    'use strict';

    // demo code: use below code when your bower component needs templates
    // $templateCache.put('views/directive/testDirectiveTemplate.html',
    //   "<h1>{{greeting}}</h1>\n"
    // );

}]);
